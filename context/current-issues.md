read and analyze this image screenshots/autosave.png find it's root cause.

Check the /hooks/use-canvas-autosave.ts function analyze it carefully, when i change something it does'nt autosave on the vercel blob storage it only shows the error on the image i provided.

Reminder:
I changed the access to private, because when i setup the vercel blob it was on private.